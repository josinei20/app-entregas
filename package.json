{
  "name": "delivery-docs-app",
  "version": "1.0.0",
  "description": "Delivery Documentation App for GeoTransportes",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "setup": "echo 'Instalando dependências...' && cd backend && npm install && cd ../frontend && npm install && echo '✓ Setup completo!'",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "prestart": "npm run build:frontend",
    "build": "npm run build:frontend && cd backend && npm install",
    "build:frontend": "node build.js",
    "start": "node backend/src/server.js",
    "heroku-postbuild": "npm run build",
    "postinstall": "node build.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
