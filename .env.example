# ========================================
# Backend Configuration
# ========================================

# Database
MONGODB_URI=mongodb://localhost:27017/delivery-docs
# Para MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/delivery-docs

# JWT Secret (use algo bem longo e seguro em produção)
JWT_SECRET=sua_chave_secreta_bem_forte_aqui_colocar_32_caracteres_aleatorios

# Server
PORT=5000
NODE_ENV=development
# development, production, test

# ========================================
# Frontend Configuration
# ========================================

# API URL
REACT_APP_API_URL=http://localhost:5000/api
# Em produção: https://seu-dominio.com/api

# ========================================
# Notas
# ========================================
# 1. Criar arquivo .env na RAIZ do projeto (não dentro de backend/frontend)
# 2. Não fazer commit deste arquivo (está no .gitignore)
# 3. Para produção, usar variáveis de ambiente seguras
# 4. JWT_SECRET deve ser único e complexo para cada ambiente
